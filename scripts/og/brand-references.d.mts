/** Types for `brand-references.mjs`, which stays plain JS so build scripts can run it directly. */

export type ReferenceImage = { file: string; note: string };

export declare const referenceImages: readonly ReferenceImage[];
