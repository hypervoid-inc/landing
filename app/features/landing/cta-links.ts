/**
 * Low-commitment CTA shared by the homepage hero and `/launch`.
 *
 * Most first-time readers will not start a trial on the visit that brought
 * them here; without a second option their only exit is to leave. This is the
 * same intake form the GTM email offers, so the site keeps that promise.
 */
export const WALKTHROUGH_URL = "https://forms.gle/fpu7xAUbBC6EQKzQ6";
